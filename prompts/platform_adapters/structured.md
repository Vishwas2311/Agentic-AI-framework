# Structured File Extraction

Parse JSON and YAML into normalized metadata, classify whether the source behaves like API contracts, low-code metadata, or generic configuration, and emit confidence with unsupported areas.
