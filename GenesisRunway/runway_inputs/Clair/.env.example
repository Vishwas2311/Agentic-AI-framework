# Server-only. Do not expose this as NEXT_PUBLIC_OPENAI_API_KEY.
OPENAI_API_KEY=your_openai_api_key_here

# Optional. Defaults to gpt-4.1-mini when omitted.
OPENAI_MODEL=gpt-4.1-mini

# Optional. Defaults to 15000 so slow AI calls fall back to local PDF extraction.
OPENAI_TIMEOUT_MS=15000
