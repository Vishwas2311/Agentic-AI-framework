# QA Prompt

Run deterministic checks before asking any agent to fix code.

Collect:

- build failures
- unit/API test failures
- Playwright failures
- visual diff failures
- accessibility failures
- security findings
- SBOM/vulnerability findings

Then route concrete failures to scoped agents.

